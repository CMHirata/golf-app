// ─── src/constants/layout.js ──────────────────────────────────────────────────
// Shared layout constants used by App.jsx, ScorecardPage.jsx, and ResultsPage.jsx.
// All three files must agree on these values — change here, not in each file.

// Fixed bottom nav bar height in px (excludes safe-area-inset-bottom).
export const NAV_BAR_HEIGHT    = 58;

// Pinned action bar height in px, used by Scorecard and Results pages.
export const ACTION_BAR_HEIGHT = 52;
